n=input()

inn=int(n)      // n=int(input())로 축약 가능+ 변수의 개수 -1
count=0
for i in range(inn+1):
    for j in range(60):
        for k in range(60):
            time=str(i)+str(j)+str(k)
            if "3" in time:
                count+=1
            k+=1
        j+=1
    i+=1

print(count)   
