n=input()

inn=int(n)
count=0
for i in range(inn+1):
    for j in range(60):
        for k in range(60):
            time=str(i)+str(j)+str(k)
            if "3" in time:
                count+=1
            k+=1
        j+=1
    i+=1

print(count)   
